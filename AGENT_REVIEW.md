# Agent 评审记录

> 对每个 Agent 的岗位说明书做优缺点分析，记录优化方向和落地状态。
> 已完成优化的标记 ✅，建议中的标记 💡。
> Agent 总数：8（OL / WB / 主编 / A / B / C / D / E）

---

## Outliner（大纲师 OL）

> 分析日期：2026-08-07 | 新建，待实战验证

### 功能定位

Phase 1 规划调度——陪作者把模糊想法变成结构化的大纲和卷纲。交互模式为对话式（提问→听→建议→等反馈），和主编的批处理调度完全区分。

### 设计要点

1. **交互模式分离是核心决策**：大纲规划和写作调度是两种互斥的 AI 行为模式，塞进同一个 prompt 两头都做不好
2. **一次只问一段**：防止 AI 一次性砸出所有空白让作者不知所措
3. **建议带理由**：不直接给答案，说清楚取舍，让作者做决定
4. **每轮写回文件**：不在对话里飘着，每步都落地

### 待观察的风险

1. **逐段推进可能太慢**：如果作者已经想清楚了大部分内容，逐段反而效率低。需要支持"一次性填完初稿，AI 整体反馈"的快捷模式
2. **卷纲一致性检查可能过于机械**：爽点类型不连续 2 章重复、每 3 章一个爽点——这些规则对某些叙事风格可能是枷锁
3. **没有和 WorldBuilder 的联动**：大纲里的修炼体系如果和 WorldBuilder 检索到的典籍有冲突，目前 Outliner 不知道

---

## Outliner（大纲师 OL）

---

## BetaReader（读者 D）

> 分析日期：2026-08-04

### 优点

1. **真正盲读视角**：不看设定和大纲，模拟真实读者状态。与 Reviewer（B）互补——B 看过设定后对"信息量大"的段落容忍度高，D 不受此影响，是 B 的校准器。
2. **抓住核心指标**：prompt 明确"会不会点下一章"是最高权重问题，契合网文连载商业模式。
3. **输出模板简洁聚焦**：感受评分 / 想跳过 / 钩子测试 / 看不懂 / 最爽瞬间，五个维度各有明确用途。
4. **成本极低**：输入仅需章节正文，无需组装上下文、RAG 检索、读设定文件。
5. **触发策略合理**：只在关键章节（开篇/高潮/卷末）使用，不每章都跑。
6. **验证信息自包含性**：不看设定的读者能否看懂正文——直接测试了写手是否在叙事中自然传达了必要信息。

### 缺点

1. **无前文记忆**：每章冷启动，会把前文已解释的概念标记为"看不懂"，假阳性率高。
2. **读者画像模糊**："普通网文读者"无固定偏好，不同章节可能用不同标准，反馈一致性差。
3. **1-5 分制与 B 的 1-10 不对齐**：主编交叉对比时 scale 不统一。
4. **"想跳过"来自 LLM 模拟而非真实行为**：LLM 不真正跳读，标记的可靠性存疑。
5. **缺少情绪曲线**：单点评分丢失了章节内部结构（开头炸结尾垮 vs 全程平稳）。
6. **只有诊断无处方**：标记了问题但不像 Reviewer 那样给出修改建议。
7. **单一读者**：缺少群体多样性，一个人的反馈容易被误当真理。
8. **不区分困惑类型**：悬念制造的"好困惑"和信息缺失的"坏困惑"混在一起。

---

### 优化方向

#### ✅ 1. 前情摘要（已应用）

**问题**：D 每章冷启动，把前文已解释的概念标记为"看不懂"，假阳性噪音大。

**方案**：输入中新增「前情摘要」块（100-150 字），不是设定文档，只是"追到这一章的读者已知的故事信息"。主编从 `_outline/{book}/novel_state.yaml`（当前书）自动生成。同时「身份」段改为"你看过前面的章节，记得故事来龙去脉——但没看过设定文档"。

#### ✅ 2. 拆分「看不懂」为两种困惑（已应用）

**问题**：悬念制造的"好困惑"和信息缺失的"坏困惑"混在一起，主编无法判断严重性。

**方案**：「看不懂的地方」拆为 `😕 信息不够`（作者没给够，需修）+ `🤔 悬念好奇`（作者故意留的钩子，不需修）。

#### 💡 3. 定义读者画像

**问题**：无固定读者画像，反馈缺乏一致性锚点。

**方案**：「身份」段加固定画像——追了三年仙侠的老读者，明确喜欢（合理升级、有代价的爽、角色博弈）和不喜欢（无脑打脸、设定说明文、独角戏）。

#### 💡 4. 加入情绪波动标记

**问题**：单点评分丢失章节内部结构。

**方案**：评分后加一行可选标记——`情绪波动：开头平淡 → 中段揭露时兴奋 → 结尾对话太长有点泄气`，三个关键节点即可。

#### 💡 5. 评分对齐 10 分制

**问题**：D 用 1-5，B 用 1-10，交叉对比 scale 不对齐。

**方案**：D 改为 10 分制，附锚定参考（6-7=合格会追不会安利，8-9=主动追更，10=截图发群）。

---

> 已应用变更详见 `_agents/beta-reader.md` 当前版本。
> 主编侧待配套：`orchestrator.md` 调用 D 时需从 `_outline/{book}/novel_state.yaml` 拼前情摘要。

---

## Writer（写手 A）

> 分析日期：2026-08-05

### 优点

1. **核心原则有哲学内核**：「知识有根」「拾级而上」「爽点落地」「典籍为料」——四条不是写作技巧，是创作哲学。特别是"爽感来自用正确方法做到别人做不到的事，不是靠系统面板"——直接定义了这本书的爽点风格。
2. **RAG 使用写到了可执行级别**：不是"查一下典籍"，而是具体到场景→关键词映射表（写炼丹→搜"抱朴子 炼丹 炉火"）。并且教了怎么用检索结果：不抄原文、保留核心数据、融入动作而非旁白。
3. **硬约束每一条都能量化检查**：AI 句式和独白长度（E+B 共同覆盖）、标题字数（脚本机械检）、开头 300 字入正题（B 可判断）——没有一条是空话。
4. **「缺失设定标记」建立了 Writer→WB 闭环**：Writer 发现设定不够用时不硬编，而是标出来，触发 WorldBuilder 增量补充。
5. **标点标准量化**：逗句比 ≥ 3.0、句长 15-30 字——给了 Writer 具体的节奏目标。

### 缺点

1. **约束密度过高**：8 条硬约束 + 5 条标点标准 + 核心原则——Writer 拿到 prompt 第一感觉是"我不能做什么"，可能过度谨慎导致文字正确但平淡。
2. **「不自行创造新设定」边界模糊**：苍云宗有内门外门——Writer 可以写外门食堂长什么样吗？可以写宗门里的树吗？"不创造设定"和"正常环境描写"之间的边界没有定义，Writer 可能因害怕越界而不敢写环境，导致场景空洞。
3. **缺少章节内部结构指引**：2000-2500 字的章节，开头/中段/结尾各占多少？爽点放在什么位置？钩子留多大篇幅？当前只有"开头 300 字进核心事件"和"结尾必须有钩子"，中间全靠 Writer 自己。
4. **POV 没有约束**：网文里 POV 跳转是最常见的阅读体验杀手——一段主角视角、一段配角心理、一段上帝视角混在一起。Writer prompt 没提 POV 一致性。
5. **文风样本依赖主编选择**：主编选的样本不匹配场景，Writer 的参照系就偏了——但 Writer 自己不知道样本选得好不好。

---

### 优化方向

#### ✅ 1. 约束分层（已应用）

**问题**：8 条硬约束混在一起，Writer 感到压迫。

**方案**：分为两级——「红线」（AI句式/新设定/标题字数/POV/标点，E 检查）+「创作指引」（独白/描写密度/钩子/角色对话/战斗/开头，B 评审）。

#### ✅ 2. 明确「不创造设定」的边界（已应用）

**问题**：Writer 可能因害怕越界而不敢写环境细节。

**方案**：加豁免说明——环境描写、临时龙套、过渡物品不属于"创造设定"，核心判断标准："如果这个细节会被后续剧情引用、或会影响角色能力/关系——那就是设定，必须来自 bible/。"

#### ✅ 3. 补充章节结构指引（已应用）

**问题**：2000-2500 字怎么分配没有指导。

**方案**：加「章节结构参考」——开头 200-300 字（钩子/接续）→ 中段 1200-1600 字（核心事件+爽点+信息传递）→ 结尾 200-300 字（钩子，优先信息揭示或关系变化）。推荐比例，非硬性。

#### ✅ 4. 增加 POV 约束（已应用）

**问题**：无视角一致性要求。

**方案**：红线中加——"每章保持单一视角。切换用 `---` 标记，每章不超过 1 次。"

---

> 已应用变更详见 `_agents/writer.md` 当前版本（2026-08-05）。

---

## Reviewer（审稿 B）

> 分析日期：2026-08-05

### 优点

1. **每个维度有一个"杀手级"检测项**：爽点→套路重复检测、文笔→删前两句测试、水文→给出具体反例对话、节奏→连续 2 章平淡报警。每个维度都有可操作的抓手，不是抽象评价。
2. **评分标准有锚定**：1-3=严重、6-7=合格不亮眼、10=爆款——不同章节的评分之间可比，主编可以追踪趋势。
3. **输出有「亮点」段**：不只挑毛病，还告诉 Writer 什么写得好、要保持——Writer 改的时候不会把好的也改没。
4. **修改建议可操作**：段落定位 → 问题描述 → 具体怎么改。
5. **通过标准合理**：四维中至少三维 ≥ 6/10，无维度 < 4/10。允许有一维平庸，不容忍任何一维严重翻车。

### 缺点

1. **两个检查项需要前文信息但输入里没有**：「套路是否和最近 5 章重复」和「连续第 2 章平淡」——B 只能看到当前章，不知道前文。主编需要把"前 5 章爽点模式摘要"和"上一章 B 评分"传进 B 的上下文。
2. **AI 句式列表是静态枚举**：「不仅如此」「与此同时」是 GPT-3 时代的指纹，新模型有不同的习惯表达。枚举永远在追——应该教 B 识别特征（主谓宾过于完整、连接词过多、缺乏口语节奏），而不是靠一个固定列表。
3. **「删掉前两句」测试对 LLM 不可执行**：LLM 没法真正"删掉看看"，只能是模拟——模拟结果和真实人类阅读体验可能不一致。
4. **缺少和 D（BetaReader）的校准意识**：agent-notes 已记录 B/D 评分偏离案例，但 B 自己的 prompt 完全没提这件事。如果 B 知道"有个不看设定的读者也在审，我的评分会和她对比"，审稿心态可能不同。
5. **通过阈值可能过低**：四维全 6 分可通过——6 分是"合格但不亮眼"。如果连续多章全 6 分，累积效果是"全程平庸"。需要一道"连续 N 章 B 全 6 分 → 告警"的机制。
6. **文风样本对比依赖主编的样本选择**：主编选错样本 → B 基于错误参照系打文笔分 → 系统性偏高或偏低。

---

### 优化方向

#### ✅ 1. AI 句式从枚举升级为特征识别（已应用）

**问题**：固定列表永远追不上模型进化。

**方案**：在维度二的 AI 味检测中增加特征描述——"AI 味的句子通常：主谓宾结构过于完整、每句都有明确的连接词（因此/于是/随后）、缺乏口语化的省略和倒装、节奏均匀缺少长短变化。不限于下方示例列表，符合这些特征也标注。"

#### ✅ 2. 增加 B/D 校准提示（已应用）

**问题**：B 不知道自己的评分会被 D 交叉验证。

**方案**：在 B 的「通过标准」后加提示——"关键章节会有盲读读者同时阅读本章。如果你给高分但读者反馈差，你的评分标准将被重新审视。"

#### ✅ 3. 连续平庸告警（已应用）

**问题**：四维全 6 分每章都能过，但连续 5 章全 6 分说明小说在"安全区"里滑行。

**方案**：在 B 的输出格式中增加一个可选字段——"平庸信号：本章是否有任何一维达到 8 分以上？如连续 3 章无 8 分维度，标记「持续平庸」。"这项不阻塞通过，但提醒主编和人类作者关注。

#### 💡 4. 补充前文信息输入（主编侧）

**问题**：B 的「套路重复」和「连续平淡」检查需要前文信息，但当前输入里只有当前章。

**方案**：主编在组装 B 的上下文时额外传入——"前 5 章爽点模式摘要"（每章一句话）+ "上一章 B 各维评分"。这是 orchestrator.md 的改动，不涉及 reviewer.md。

---

> 已应用变更详见 `_agents/reviewer.md` 当前版本（2026-08-05）。

---

## Inspector（校验 C）

> 分析日期：2026-08-05

### 优点

1. **五个维度覆盖精准**：境界、金手指、角色、伏笔、时间线——全是网文连载中纯人类写作的高频事故点。维度设计没有废话。
2. **年龄锚点规则务实**：从 Writer 5 次推算错误的血泪教训里长出来的规则——"逐条核对 age_anchors，禁止凭感觉判断"。不是纸上谈兵。
3. **每条矛盾带设定依据**："矛盾描述 → 设定依据 → 修复建议"——Writer 不需要翻设定文件就能定位修改。比 D 的"只诊断没处方"更进一步。
4. **伏笔遗忘告警**：超 30 章未提及自动提醒。人类写作最容易出的问题，机器天然比人靠谱。
5. **增量输入策略**：每 10 章只查最近 10 章，卷末才扫全卷——在精确性和 token 成本间取了平衡。
6. **输出分级和 E/B 一致**：🔴 大矛盾 / 🟡 小矛盾 / ⚠️ 告警 / ✅ 确认，主编拿到就能决策。

### 缺点

1. **10 章间隔太久（已修复）**：第 1 章埋的矛盾到第 10 章才发现，修复成本指数级上升。→ 已改为五层节奏。
2. **卷末用摘要可能漏细节**：200 字摘要会吞掉角色外貌、说话风格等细节矛盾。卷末适合查大局，细节一致性交给 3/5/10 章检查。
3. **不查设定 × 设定矛盾（已修复）**：Inspector 原来只比对章节 vs 设定，不比对设定 vs 设定。→ 10 章大检新增"设定内部矛盾"检查。
4. **"无意回收伏笔"检测靠 LLM 判断**：语义判断门槛高，漏报误报概率大。10 章大检中增加了"可能已过期"标记来对冲——宁多报不漏报。
5. **时间线检查缺参照表**：novel_state 的 timeline 只记大事，日常接续时间需要 Inspector 自己在章节间逐句比对。
6. **角色"说话风格"检查太软**：对话风格不符合设定是深度理解问题，Inspector 可能给出模糊反馈，对 Writer 可操作性差。

---

### 优化方向

#### ✅ 1. 五层检查节奏（已应用）

**问题**：原来只在每 10 章和卷末触发，低级矛盾发现太晚，修复成本高。

**方案**：

| 层级 | 触发 | 检查维度 | 输入量 | 负责人 |
|------|------|---------|--------|--------|
| 每章微型 | 每章定稿后 | 境界匹配 + 时间接续 | 0（不调 Agent） | 主编 |
| ▲ 3 章轻检 | ch3/6/9/12... | 角色外貌/说话风格/3 章内时间/伏笔推进 | 3 章全文 + 角色表 | Inspector |
| ■ 5 章中检 | ch5/10/15... | 金手指规则/境界节奏/角色关系/伏笔状态 | 5 章全文 + 三大设定 + 伏笔表 | Inspector |
| ★ 10 章大检 | ch10/20/30... | 全部 5 维 + 年龄算术 + 设定内部矛盾 | 10 章全文 + 全部设定 + age_anchors | Inspector |
| ◆ 卷末全局 | 卷末 | 大局一致性/伏笔收尾/角色弧光/废设定告警/节奏概览 | 全卷摘要 + 全部设定 | Inspector |

**重叠规则**：ch10 同时触发 3/5/10 三档时只跑最高级（10 章大检），不重复。

#### ✅ 2. 新增「设定 × 设定」矛盾检查（已应用）

**问题**：Inspector 原来把设定 YAML 当绝对正确，不检查设定文件之间的规则冲突。

**方案**：10 章大检新增第 6 维——逐条比对 realm-system / golden-finger / world-structure 之间是否有冲突规则。卷末全局延续此项。

#### ✅ 3. 明确大/小矛盾分界（已应用）

**问题**：原来"大矛盾"和"小矛盾"没有判断框架。

**方案**：大矛盾 = 影响后续剧情逻辑或违反核心规则；小矛盾 = 不阻塞剧情但影响一致性。附加 `[存疑]` 机制——可能是作者刻意设计时标注存疑而非判错。

#### 💡 4. 卷末检查改用分层输入

**问题**：全卷摘要会吞细节，但全卷全文塞不进 prompt。

**方案**：卷末检查改为两份输入——全卷摘要（给大局一致性、节奏概览用）+ 全卷的角色出场/伏笔/时间线统计表（给细节一致性用）。统计表由主编根据每章的「本章状态变更摘要」自动维护，不需要 Inspector 从全文里扒。

#### 💡 5. 检查结果的结构化存档

**问题**：当前每份 Inspector 报告是 markdown 文本，主编对比两份报告时只能靠人眼。

**方案**：Inspector 报告末尾附加一个 YAML 摘要块（矛盾数、级别、涉及的设定条款），主编可以直接 diff 两份 YAML 做趋势分析（"这 10 章的矛盾数上升了"）。

#### 💡 6. 中期补一次"只看角色状态 + 境界"的抽查

**问题**：3 章检不查境界和金手指，如果 3-5 章之间出现境界矛盾，要到第 5 章中检或第 10 章大检才暴露。

**方案**：这不是紧急问题（3 章跨度境界不太可能出错），但可以在主编调度规则中加入——如果 3 章轻检报告中有任何角色状态异常，主编主动加一次手动抽查。

---

> 已应用变更详见 `_agents/inspector.md` 当前版本（2026-08-05 重构）。
> 主编侧待配套：`orchestrator.md` 需加入每章微型检查 + Inspector 分层触发调度逻辑。

---

## FormatChecker（格式检查 E）

> 分析日期：2026-08-04

### 优点

1. **职责边界极度清晰**："只查规则，不评判好坏"贯穿全文。E 明确知道自己不是 Reviewer，不做内容评判。
2. **与脚本分工精准**：`verify_punct.py` 做机械统计，E 做上下文判断，互不抢活不留真空。
3. **存疑机制**：边界情况标注 `[存疑]`，不强行判错——这种克制在多 Agent 系统里少见。
4. **三级问题分级**：🔴 必须修 / 🟡 建议修 + 每条定位到段落句子，Writer 拿到就能改。
5. **输出含定量数据**：逗句比、句长、短句比，主编可横向对比不同章节的格式质量趋势。
6. **与 Reviewer 并行执行**：E 和 B 在 Writer 完成后同时跑，不拖慢写作循环。

### 缺点

1. **AI 味句式越界**（已修复）：第四类禁止项的"不仅如此""与此同时"本质是文笔判断不是格式检查，且 Reviewer 维度二已覆盖。
2. **段落密度越界**（已修复）："连续 3 段无对话或动作"应归属 Reviewer 维度三。
3. **「句号滥用」标准主观**："可以用逗号承接却用了句号"需要文感而非规则判断，与 E 的规则检查员身份有张力。
4. **输入依赖 Writer 规则且无版本锁定**：主编漏传新版规则会导致 E 基于旧标准检查。
5. **缺少特殊段落豁免指引**：战斗场景碎片句、对话内不完整句——写了"刻意节拍除外"但没教 E 怎么区分。
6. **存疑项缺裁决闭环**：E 标 `[存疑]` 后谁终裁？主编不一定懂标点，需一条"→ 人类作者终裁"路径。
7. **统计数据缺历史对比**：逗句比 1.8 单看达标，但如果前 10 章都是 3.5，1.8 是 Writer 状态变化的信号，E 不做趋势分析。

---

### 优化方向

#### ✅ 1. 逗句比收紧至 3.0（已应用）

**问题**：1.5 阈值太低，AI 味文本仍然可能达标。

**方案**：全局修改 `1.5 → 3.0`，涉及 `format-checker.md`、`writer.md`、`verify_punct.py`、`DESIGN.md`、`README.md`、章纲模板，共 6 个文件。

#### ✅ 2. 砍掉 AI 句式 + 段落密度，还给 Reviewer（已应用）

**问题**：AI 句式和段落密度是内容判断不是格式检查，且 Reviewer 已覆盖，E 在重复劳动。

**方案**：「第四类：禁止项」中移除 AI 句式和段落密度，末尾加备注说明已归属 Reviewer。E 只查标点格式。

#### ✅ 3. 符号检查全面化（已应用）

**问题**：原有四类覆盖不全——缺分号、问号/叹号、破折号、书名号、全角半角、概数顿号规则、引号嵌套规则。

**方案**：四类检查清单扩展：

| 类别 | 原有 | 新增 |
|------|------|------|
| 第一类 句末停顿 | 逗号、句号 | + 问号/叹号连用、问叹连用规则 |
| 第二类 句内停顿 | 顿号、冒号 | + 分号使用、概数不用顿号 |
| 第三类 标号 | 引号配对、对话标点 | + 引号风格统一、引号嵌套、省略号去冗余、破折号规范、书名号规范 |
| 第四类 禁止项 | 英文标点混用、标题字数 | 精简为半角标点混用 + 标题字数，砍掉 AI 句式/段落密度 |

#### 💡 4. 补充特殊段落豁免指引

**问题**：E 会把战斗碎片句、对话内不完整句误标为"短句碎片"。

**方案**：在「核心原则」或「第一类」中加豁免说明——战斗场景的动作节拍链、对话内的应答短句、主角内心震惊时的碎片句，标 `[刻意]` 不判错。同时教 E 通过上下文（是否有连续动作、对话是否在推进）区分刻意与无意。

#### 💡 5. 存疑项裁决闭环

**问题**：E 标 `[存疑]` 后无人终裁。

**方案**：主编汇总报告时，存疑项单独列出 → 人类作者终裁。如果作者不回复，主编默认按 `🟡 建议修` 处理，不阻塞通过。

#### 💡 6. 统计数据加历史对比

**问题**：单章数据缺少参照系。

**方案**：主编在传给 E 的输入中附带"前 3 章统计均值"，E 在报告中输出"本章 vs 前 3 章均值"对比行。

---

> 已应用变更详见 `_agents/format-checker.md` 当前版本（2026-08-04 重构）。

---

## Orchestrator（主编）

> 分析日期：2026-08-05 | 初版重写，待实战调校

### 背景

旧 `orchestrator.md` 实际内容是"典籍研究员"（用 RAG 检索典籍 → 输出设定 YAML），完全没有 Phase 3 写作调度逻辑。A/B/C/D/E 五个 Agent 的岗位说明书都写好了，但调度它们的人没有岗位说明书。2026-08-05 将旧内容拆出为 WorldBuilder，重写了此文件。

### 优点

1. **上下文剪裁下沉到主编**：每个 Agent 拿到的输入不同（A 需要章纲+设定+RAG，D 只需要前情摘要+正文，E 只需要正文+格式规则）——主编按需组装，避免 Agent 读到不该读的内容。
2. **E+B 并行设计**：格式检查和内容审稿互不依赖，并行跑节省时间。
3. **修改循环有硬终止条件**：最多 3 轮，3 轮不过标记人工介入——不像一些 Agent 系统无限循环直到 prompt 窗口炸掉。
4. **跨 Agent 校准规则明确**：B 高分 D 差评 → 采纳 D。C 发现大矛盾 B 未察觉 → 补充进 B 下一轮上下文。不是各打五十大板，有明确的优先级。
5. **Inspector 分层触发规则化**：按 3/5/10 章固定节拍触发，主编不需要自己判断"该不该查"。
6. **前情摘要生成逻辑落地**：调用 D 前从 novel_state.yaml 自动拼前情摘要——BetaReader 重构时定义的需求，主编侧落地。
7. **每章微型检查不调 Agent**：境界匹配+时间接续主编自己做，零成本拦截最频繁的低级错误。

### 缺点 / 待观察风险

1. **上下文组装 token 成本**：主编每章要读章纲+三大设定+前文+novel_state+文风样本，输入量本身不小。如果主编自身成了 token 消耗大头，需要考虑"主编只看摘要版"。
2. **3 轮修改终止后无自动降级**：只标记"需人工介入"，如果人类不在线，章节就卡住了。是否需要"取 3 轮中 B 评分最高版为暂定稿"？
3. **Inspector 大矛盾暂停写作的连锁反应**：如果矛盾修复需要回溯修改前几章，边界在哪？回修完了要不要重新审稿？
4. **prompt 膨胀风险**：后续调校经验会追加到 orchestrator.md 和 agent-notes.md，需要警惕臃肿导致调度决策质量下降。
5. **实战未经验证**：整个调度流程是基于设计推演的，尚未在连续多章写作中跑过——实际运行中几乎一定会暴露设计盲区。

---

### 优化方向

#### ✅ 1. 拆分设定建造职责（已应用）

**问题**：旧 orchestrator.md 只覆盖 Phase 1 设定建造，Phase 3 写作调度完全缺失。

**方案**：旧内容 → `world-builder.md`，新建 `orchestrator.md` 覆盖 Phase 3 全部调度逻辑。

#### ✅ 2. 上下文组装标准化（已应用）

**问题**：原来没有明确定义各 Agent 的输入规范。

**方案**：主编 prompt 中逐条列出了 A/B/C/D/E 各自的输入要求、组装方式、文件路径。

#### ✅ 3. 周期性触发落地（已应用）

**问题**：Inspector 分层节奏、BetaReader 前情摘要、WorldBuilder 增量触发——这些在其他 Agent 重构时定义的需求，需要在主编侧有对应的调度指令。

**方案**：主编 prompt 中落实了全部触发逻辑和输入组装方式。

#### ✅ 4. 新增章纲生成职责（2026-08-07 已应用）

**问题**：章纲是 Writer 的必备输入，但整个系统里没有任何 Agent 负责生成章纲。Writer 自己写 = 利益冲突；新建章纲师 = 过早优化。

**方案**：单章循环新增「第零步：确认章纲存在」——章纲不存在时主编先生成再进入写作流程。含章纲模板（目标/节拍/伏笔/情绪/钩子）+ 四条质量约束。

#### 💡 5. 主编自身上下文减负

**问题**：每章都要读全部设定+前文，token 消耗线性增长。

**方案**：后期如果发现主编 token 占比过高，可以改为——三大设定只读"与本章相关的条款"（根据章纲关键词筛选），前文只读"前 1 章全文 + novel_state 摘要"而非前 10 章摘要。

#### 💡 6. 修改轮次的自动降级

**问题**：3 轮修改不通过 → 标记人工介入，人类不在线就卡住。

**方案**：增加自动降级选项——取 3 轮中 B 四维总分最高的版本为"暂定稿"，标注"待人类确认"，不阻塞后续章节。人类上线后可逐章审核、决定是否回修。

---

> 已应用变更详见 `_agents/orchestrator.md` 当前版本（2026-08-05 重写）。

---

## WorldBuilder（设定研究员 WB）

> 分析日期：2026-08-05 | 从旧 orchestrator.md 拆分而来

### 背景

旧 `orchestrator.md` 的实际内容是"用 RAG 检索典籍 → 归纳结构化 → 输出设定 YAML"，本质是设定研究员而非写作调度器。2026-08-05 拆分为独立 Agent，并增加了写作中增量触发模式。

### 优点

1. **检索方法论扎实**：同一主题多角度检索、跨书对比、换关键词重试——不是只丢一个 rag_query 命令，而是在教 Agent 怎么做学术检索。
2. **出处标注是反幻觉机制**：每条设定标注典籍出处，不确定的标 `[待确认]`，检索不到的标 `[典籍未找到]`——三档标注有效抑制 LLM 编造。
3. **两种触发模式覆盖全生命周期**：开书时完整建造 → 写作中按需增量补充（Writer 标缺什么补什么），不是"做完就扔"的一次性工具。
4. **增量模式"只补不改"**：不碰已有设定条目，只追加新条目——避免写作中途意外修改设定导致 Inspector 报假矛盾。
5. **输出优先级合理**：境界体系 → 世界结构 → 异兽谱 → 角色人设 → 金手指，先搭骨架再填血肉。

### 缺点 / 待观察风险

1. **写作中频繁增量可能导致 YAML 碎片化**：Writer 每章报一个缺失，WB 补一条——10 章下来同一个 YAML 文件的条目风格可能不一致、条目之间可能产生新的矛盾。
2. **检索质量受 RAG 管线制约**：WB 的设定质量天花板 = `rag_query.py` 的召回精度。如果 RAG 检索不准，WB 没有替代方案。
3. **"检索不到"时的处理单一**：目前只标 `[典籍未找到]`，没有后续动作——是否允许 WB 基于相近概念做合理推演（标注为"推测"）？

---

### 优化方向

#### ✅ 1. 从主编拆分独立（已应用）

**问题**：设定建造和写作调度共用一个 Agent 名，Phase 3 调度逻辑完全缺失。

**方案**：旧内容移入 `world-builder.md`，新建 `orchestrator.md`。

#### ✅ 2. 增量触发模式（已应用）

**问题**：原来只有开书时的一次性建造，Writer 在写作中遇到设定缺失时无法求助。

**方案**：新增"写作中增量模式"——Writer 在章末「缺失设定标记」中列出需求 → 主编调 WB → WB 检索补充 → 追加到对应 YAML。

#### 💡 3. YAML 碎片化防护

**问题**：频繁增量可能导致同一个 YAML 文件的条目风格不一致。

**方案**：每 10 章由主编触发一次"设定整理"——把增量追加的零散条目按类别归类，统一格式，检查新增条目之间是否有矛盾。相当于给 WB 的产出加一道 Inspector 式的校验。

#### 💡 4. "典籍未找到"时的推演权限

**问题**：有些网文需要的设定（如"空间戒指"）在古籍里根本没有对应概念。标 `[典籍未找到]` 是正确的，但 Writer 仍然需要这个设定。

**方案**：增加"合理推演"档——如果一个概念在典籍中没有直接对应，但可以从相关概念推演（如从"袖里乾坤"推演出储物法宝的概念），标注为 `[推演·基于XX典籍的XX概念]`，与直接引用、确认缺失并列。

---

> 文件位于 `_agents/world-builder.md`（2026-08-05 从 orchestrator.md 拆分）。
